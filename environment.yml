# use conda env export --no-builds --from-history > environment.yml and then add the pip installs
name: DummyEnv
channels:
  - defaults
dependencies:
  - python=3.7
  - mysql-connector-python
  - flask
  - flask-restful
  - pip:
    - numpy
    - pandas
